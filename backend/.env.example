DATABASE_URL=sqlite:///./app/duolingo.db
# Comma-separated list of allowed frontend origins in production, e.g.
# CORS_ORIGINS=https://your-app.vercel.app
# Leave unset for "*" (fine for local dev / before the frontend URL is known).
CORS_ORIGINS=
